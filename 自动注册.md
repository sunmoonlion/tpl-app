# Casdoor 自助注册边界

Architecture v2 中，Admin 与 Web 两个 Next.js 表面共用 `tpl-backend`，但必须使用不同的
Casdoor application、redirect URI、cookie 名和 session namespace。

只有 Web 产品面可以按产品策略暴露自助注册链接。Admin 管理面默认不提供公开注册入口，账号由
Casdoor 管理员或受控邀请流程创建。

推荐链路：

```text
Web 注册入口
  -> Casdoor Web application signup
  -> Casdoor Authorization Code + PKCE
  -> /api/auth/web/callback
  -> Backend 建立 Web session
```

实现要求：

- 注册 URL 必须由服务端根据 Casdoor issuer、organization 和 Web application 生成；
- 禁止把 client secret、access token 或 refresh token写入浏览器 bundle/localStorage；
- callback 必须校验 state、nonce、PKCE、redirect URI 与 issuer；
- 注册成功只同步本地影子用户，认证和全局身份仍以 Casdoor 为权威；
- Backend 仍对每个业务请求执行 scope、资源所有权、Origin/CSRF 校验；
- Admin/Web 注册与登录门禁必须分别测试，不能用 Web 成功推断 Admin 安全。

实际路由和配置名以 `tpl-backend` 当前代码、OpenAPI 和 contract tests 为准，不在本说明复制第二份
接口事实。
