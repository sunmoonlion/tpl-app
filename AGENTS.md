# 项目入口指针

本仓是 SunMoonAI 平台五个协作仓之一（`tpl-app` / `info-app` / `knowledge-app` /
`investment-app` / `k8s`，**必须并列放置**）。

**动代码前必读**——代码必须符合的规则（39 条，按主题分组）：

`../k8s/sunmoonai/docs/dev-agent/SDD/constraints.md`

违反其中任一条的方案不进入讨论。用法见该文件「怎么用」。

要先了解项目长什么样，读项目总览：

`../k8s/sunmoonai/docs/project-guide/overall-architecture.md`

产品合同：

`../k8s/sunmoonai/docs/product/product-contract.md`

任何开发任务都要遵守的通用开发规范：

`../k8s/sunmoonai/docs/dev-human/README.md`

开发 Agent 接任务前必须读取通用开发规范里自己那一类的规则：

`../k8s/sunmoonai/docs/dev-human/`（PRD、SDD、IMP、UAT 各一份）

涉及人的批准、裁量或终审时，同时读取（人介入、权力表、审批档位）：

`../k8s/sunmoonai/docs/dev-agent/SDD/modules/0001-backend/SDD/agent-dev-guide.md`

不在此复述其中规则。
