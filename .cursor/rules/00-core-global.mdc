---
description: tpl-app Architecture v2 三组件模板仓库全局协作规则
alwaysApply: true
---

# tpl-app 全局规则

本仓库是**父模板仓**，默认聚合统一 FastAPI Backend 与两个 Next.js 前端。API、Worker、Scheduler、Migration 是同一 Backend 镜像的运行角色，不是独立源码组件。执行 `init.sh` 后三个活动子模块会变为 `<项目名>-backend`、`<项目名>-admin-frontend` 和 `<项目名>-web-frontend`。

- 目标优先级：先跑通主链路，再做重构和升级。
- 每次改动都要可验证：提供启动步骤、验证命令、预期结果。
- 不破坏现有可运行基线：未确认前不主动升级 Python/Node/核心依赖。
- 接口优先对齐：前端字段、状态码、鉴权头严格以后端为准。
- 小步提交：单次变更聚焦一个问题，避免混合「功能+重构」。
- 安全底线：禁止新增明文密钥/密码，配置进入环境变量或本地私有配置。
- 文档同步：新增模块或命令时，更新对应子模块 README 或父仓说明。
- 执行风格：默认高自主执行；仅在高风险/不可逆操作前确认（如重写历史、强制推送、大规模删除、破坏性覆盖）。
