---
description: Admin/Web/Internal 共用的 FastAPI Backend
globs: **/*backend/**/*.py
alwaysApply: false
---

# 统一 Backend Python

- 兼容优先：保持现有路由前缀与响应结构；变更需评估联调影响。
- 变更原则：先补缺失能力，再做代码清理；避免一次性大改。
- 错误处理：统一返回可读错误，不吞异常；日志包含关键上下文。
- 配置管理：通过环境变量或 `pydantic-settings` 注入，不在代码写死凭据。
- 依赖控制：不随意升级 SQLAlchemy/FastAPI 等核心库，升级需单独任务。
- 可测试性：新增接口至少提供 1 条冒烟验证（请求示例 + 预期响应）。
- 运行角色：API、Worker、Scheduler、Migration 必须复用同一代码和镜像，但使用独立权限、配置与资源。
