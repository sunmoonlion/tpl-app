---
description: 管理后台 Next.js + React（tpl-admin-frontend）
globs: **/*admin-frontend/**/*.{ts,tsx}
alwaysApply: false
---

# 前端（管理端 Next.js）

- 技术栈：Next.js + React + Ant Design，以子模块当前依赖锁为准。
- 先联调后美化：先保证页面可用、接口打通，再做视觉细节。
- 接口适配：统一封装请求、鉴权、错误提示，避免页面内散落裸 HTTP。
- 与后端对齐：字段名、状态码、分页结构以后端与 `API_CONTRACT` 为准。
- 字段容错：对空值/老字段做兜底，避免白屏。
- 状态可追踪：关键列表/表单覆盖 loading / error / empty / success。
- 改动可回归：每完成一个模块，附最小手工测试步骤。
- 产品边界：保留管理端菜单、标签页、表格表单和治理交互，不把 Web 的公开 SEO 页面机械复制进来。
