---
description: 大项目交付流程（模板多仓协作）
alwaysApply: true
---

# 交付流程规则

## 1) 任务分层

- L1 目标层：明确阶段目标（如「管理端主链路」「用户端主链路」）。
- L2 模块层：按子模块与业务域拆分（认证、管理 CRUD、用户端页面等）。
- L3 任务层：每个任务可在 0.5～1 天内完成并验证。

## 2) 开发节奏

- 每次优先推进一个子模块或一条主链路，避免并行大改导致难定位问题。
- 每完成一个可交付单元必须给出：改动清单、启动方式、验证步骤、已知问题。
- 未完成验证前，不进入下一大块。

## 3) 联调门禁

- 前端联调前先从后端路由/OpenAPI/schema 和 contract tests 确认路径、方法、参数、响应结构与错误码；仓库说明见根 `README.md`，跨仓计划和契约使用任务明确指定的权威文件。
- 联调通过标准：成功路径可用 + 常见错误路径可预期（鉴权失败、参数错误、空数据）。
- 若后端不稳定，前端先用适配层兜底，避免页面内散落兼容逻辑。

## 4) 质量与回归

- 每次改动附最小回归清单（至少 3 条可复现操作）。
- 不在同一次任务中同时做「功能变更 + 大重构 + 依赖升级」。
- 历史遗留问题登记到当前权威实施计划、任务系统或对应 evidence，不创建按 AI 工具划分的问题日志，也不在当前任务无限扩展范围。

## 5) 风险控制

- 涉及配置、密钥、部署脚本的改动必须单独说明影响面。
- 升级语言运行时或核心依赖需单独阶段，前置条件是当前版本可稳定运行。
- 若阻塞超过约 30 分钟，先给替代方案（降级、mock、拆子任务）。

## 6) 交付定义（DoD）

- 功能可运行：本地可启动，主流程可操作。
- 可验证：有明确操作步骤和预期结果。
- 可维护：关键决策已记录，后续成员可接手。
