---
description: 可选 NestJS 参考实现（tpl-web-backend-nest，不进入默认实例）
globs: **/*web-backend-nest/**/*.ts
alwaysApply: false
---

# 可选 NestJS 参考实现

本目录只供对照或独立选型，不进入 Architecture v2 默认实例化、镜像发布或 K8s 运行拓扑。

- 兼容优先：保持模块边界与对外 API 契约稳定；与 Casdoor OIDC 相关配置走环境变量。
- DTO 与校验：入参使用 class-validator/class-transformer（若项目已采用），与前端字段对齐。
- 错误与日志：使用 Nest 异常过滤器/日志约定，避免裸 `console.log` 替代可观测日志。
- 依赖控制：不随意升级 `@nestjs/*` 与 ORM 驱动，升级需单独任务与回归。
- 可测试性：关键路由至少保留 e2e 或单测示例，便于模板使用者复制。
