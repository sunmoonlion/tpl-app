---
description: Shell 脚本安全与可重复执行
globs: **/*.sh
alwaysApply: false
---

# Shell 脚本规则

- 脚本默认应可重复执行（幂等），避免多次执行造成破坏性副作用。
- 涉及删除、覆盖、重置操作时，必须先做存在性检查与明确提示。
- 变量与路径必须显式引用，避免依赖隐式环境状态。
- 新增脚本需附最小使用说明（输入、输出、示例命令）。
- 禁止在脚本中写入明文敏感信息（密钥、密码、令牌）。
- 变更脚本后，在代码相邻 README 或中性 `docs/README.md` 所指向的权威运行手册中补充至少一条可复现验证步骤。
